{
  "name": "ractor-remotedev",
  "version": "0.0.5",
  "description": "Remote debugging for ractor architecture.",
  "main": "bin/index.js",
  "scripts": {
    "build": "tsc",
    "prepare": "rm -fr bin & npm run build"
  },
  "author": "corol",
  "license": "MIT",
  "dependencies": {
    "remotedev": "^0.2.9"
  },
  "peerDependencies": {
    "ractor": "^1.1.5"
  }
}
